$("#comment-of-<%= @post.id %>").html("<%= j(render partial: 'comments/comment_links', locals: {post: @post}) %>");
$("#comment_field").val('');
$("#youtube_field").val('');

<% flash.each do |type, message| %>
  $("#flash_messages").html("<div id='set_time' class='flash_class alert alert-<%= type %> alert-dismissible' role='alert'><button class='close' data-dismiss='alert'>×</button><%= message.html_safe %></div>");
<% end %>

setTimeout(function(){
  $('#set_time').fadeOut();
}, 3000);

$(".com_nil").remove();
